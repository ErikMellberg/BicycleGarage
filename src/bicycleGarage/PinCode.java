package bicycleGarage;

public class PinCode {

	/**
	 * Creates a pincode, mode either Admin or User
	 */
	public PinCode(String pin, String mode) {
		
	}
	
	/**
	 * Checks if this PinCode is equal to Pin. Equal only if both pin and mode are equal.
	 * @param pin 
	 * @return true if equal else false
	 */
	public boolean equals(PinCode pin) {
		return false;
	}
}
