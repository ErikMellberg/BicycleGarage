package bicycleGarage;

public class PinCode {

	/**
	 * Creates a pincode
	 * 
	 * @param pin
	 *            code
	 * @param mode
	 *            admin or user
	 */
	public PinCode(String pin, String mode) {

	}

	/**
	 * Checks if this PinCode is equal to x. Equal only if both pin and mode are
	 * equal.
	 * 
	 * @param x
	 *            Pincode
	 * @return true if equal else false
	 */
	public boolean equals(Object x) {
		return false;
	}
}
